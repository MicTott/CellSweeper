% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{plotClusterQC}
\alias{plotClusterQC}
\title{Plot Per-Cluster QC Distributions}
\usage{
plotClusterQC(
  spe,
  cluster_col = "cell_cluster",
  metric = "sum",
  outlier_col = NULL,
  ncol = NULL
)
}
\arguments{
\item{spe}{A \linkS4class{SpatialExperiment} object with cluster labels
and QC results.}

\item{cluster_col}{Name of the \code{colData} column with cluster labels
(default "cell_cluster").}

\item{metric}{Name of the QC metric to plot (default "sum").}

\item{outlier_col}{Name of the outlier flag column (default
\code{paste0(metric, "_outlier")}).}

\item{ncol}{Number of columns for faceting (default NULL, auto).}
}
\value{
A ggplot object.
}
\description{
Creates violin plots of a QC metric faceted by cluster, showing the
distribution within each cluster and highlighting outlier cells.
}
\examples{
library(SpatialExperiment)
library(scuttle)

counts <- matrix(rpois(2000, 5), nrow = 200, ncol = 10)
rownames(counts) <- paste0("Gene", 1:200)
coords <- matrix(rnorm(20), ncol = 2)
colnames(coords) <- c("x", "y")
spe <- SpatialExperiment(assays = list(counts = counts),
    spatialCoords = coords)
spe <- addPerCellQCMetrics(spe)
spe$cell_cluster <- rep(c("A", "B"), each = 5)

plotClusterQC(spe, metric = "sum")
}
