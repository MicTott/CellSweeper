% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{plotSpatialDispersion}
\alias{plotSpatialDispersion}
\title{Plot Spatial Dispersion}
\usage{
plotSpatialDispersion(
  spe,
  cluster_col = "cell_cluster",
  highlight_artifacts = TRUE,
  sample = NULL,
  samples = "sample_id",
  point_size = 0.5,
  use_polygons = "auto",
  polygon_col = "polygons",
  polygon_border_color = "grey30",
  polygon_border_width = 0.05
)
}
\arguments{
\item{spe}{A \linkS4class{SpatialExperiment} object with cluster labels
and artifact flags.}

\item{cluster_col}{Name of the cluster label column (default
"cell_cluster").}

\item{highlight_artifacts}{Logical. Grey out artifact clusters
(default TRUE).}

\item{sample}{Character. Which sample to plot (default NULL, first sample).}

\item{samples}{Column name for sample IDs (default "sample_id").}

\item{point_size}{Point size for point-based rendering (default 0.5).}

\item{use_polygons}{Controls polygon rendering. \code{"auto"} (default)
uses polygons if available, otherwise falls back to points. \code{TRUE}
requires polygons. \code{FALSE} always uses points.}

\item{polygon_col}{Name of the \code{colData} column containing \pkg{sf}
polygon geometry (default \code{"polygons"}).}

\item{polygon_border_color}{Border color for polygons (default
\code{"grey30"}).}

\item{polygon_border_width}{Border line width for polygons (default 0.05).}
}
\value{
A ggplot object.
}
\description{
Creates a spatial plot colored by cluster, with artifact clusters shown in
grey. Renders cell boundaries as filled polygons when available, otherwise
falls back to points.
}
\examples{
library(SpatialExperiment)
library(scuttle)

counts <- matrix(rpois(2000, 5), nrow = 200, ncol = 10)
rownames(counts) <- paste0("Gene", 1:200)
coords <- matrix(rnorm(20), ncol = 2)
colnames(coords) <- c("x", "y")
spe <- SpatialExperiment(assays = list(counts = counts),
    spatialCoords = coords)
spe <- addPerCellQCMetrics(spe)
spe$cell_cluster <- rep(c("A", "B"), each = 5)

plotSpatialDispersion(spe)
}
