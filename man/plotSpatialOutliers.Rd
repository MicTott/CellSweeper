% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-functions.R
\name{plotSpatialOutliers}
\alias{plotSpatialOutliers}
\title{Plot Spatial Outliers}
\usage{
plotSpatialOutliers(
  spe,
  metric = "sum",
  outlier_col = "cellsweeper_outlier",
  sample = NULL,
  samples = "sample_id",
  point_size = 0.5,
  colors = c("grey90", "navy"),
  outlier_color = "red",
  outlier_stroke = 0.5,
  use_polygons = "auto",
  polygon_col = "polygons",
  polygon_border_color = NA,
  polygon_border_width = 0.1
)
}
\arguments{
\item{spe}{A \linkS4class{SpatialExperiment} object with QC results from
\code{\link{clusterLocalOutliers}}.}

\item{metric}{Name of the QC metric to color by (default "sum").}

\item{outlier_col}{Name of the \code{colData} column with outlier flags
(default "cellsweeper_outlier").}

\item{sample}{Character. Which sample to plot (default NULL plots first
sample).}

\item{samples}{Column name for sample IDs (default "sample_id").}

\item{point_size}{Point size for point-based rendering (default 0.5).}

\item{colors}{Color gradient for the metric (default
\code{c("grey90", "navy")}).}

\item{outlier_color}{Color for outlier highlighting (default "red").}

\item{outlier_stroke}{Stroke width for outlier highlighting (default 0.5).}

\item{use_polygons}{Controls polygon rendering. \code{"auto"} (default)
uses polygons if available in \code{colData} and the \pkg{sf} package is
installed, otherwise falls back to points. \code{TRUE} requires polygons
(errors if unavailable). \code{FALSE} always uses points.}

\item{polygon_col}{Name of the \code{colData} column containing \pkg{sf}
polygon geometry (default \code{"polygons"}, matching SpaceTrooper
output).}

\item{polygon_border_color}{Border color for polygons (default \code{NA},
no borders).}

\item{polygon_border_width}{Border line width for polygons (default 0.1).}
}
\value{
A ggplot object.
}
\description{
Creates a spatial plot colored by a QC metric, with outlier cells
highlighted. Renders cell boundaries as filled polygons when available
(e.g., from SpaceTrooper preprocessing), otherwise falls back to points.
}
\examples{
library(SpatialExperiment)
library(scuttle)

counts <- matrix(rpois(2000, 5), nrow = 200, ncol = 10)
rownames(counts) <- paste0("Gene", 1:200)
coords <- matrix(rnorm(20), ncol = 2)
colnames(coords) <- c("x", "y")
spe <- SpatialExperiment(assays = list(counts = counts),
    spatialCoords = coords)
spe <- addPerCellQCMetrics(spe)

plotSpatialOutliers(spe, metric = "sum")
}
